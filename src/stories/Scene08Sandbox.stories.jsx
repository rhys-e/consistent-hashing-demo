import { SandboxScene } from '../components/SandboxScene';

/**
 * Scene 8: the sandbox.
 *
 * The story's claims with the numbers unlocked — server count and positions per
 * server, against the two readouts the whole thing was building towards: how far
 * from even the split is, and how much of the ring the last change moved.
 */
const meta = {
  title: 'Hash Ring/Guided Story/Scene 08 Sandbox',
  component: SandboxScene,
  parameters: { layout: 'fullscreen' },
};

export default meta;

export const Sandbox = { args: {} };
