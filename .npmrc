# Do not run dependency lifecycle hooks during install.
ignore-scripts=true

# Keep dependency resolution reproducible and explicit.
package-lock=true
save-exact=true
strict-peer-deps=true

# Do not silently use a registry inherited from a user-level config.
registry=https://registry.npmjs.org/

# Report known vulnerabilities during installs without funding prompts.
audit=true
fund=false
